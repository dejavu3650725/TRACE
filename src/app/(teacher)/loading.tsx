import { PageSkeleton } from "@/components/ui/Skeleton";

/** Shared teacher route loading state (Next.js App Router loading boundary). */
export default function TeacherLoading() {
  return <PageSkeleton />;
}
